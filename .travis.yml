language: python
python:
    - "2.7"
    - "3.2"
    - "3.3"

env:

before_install:

before_script:
    - sudo apt-get install swig

script:
    - pip install -r requirements.txt
    - python -m unittest ./tests
